// Generated/maintained constant. Overwritten by Makefile `_generate_version` before
// `make release` (so the bundled CLI bakes in the release build number).
// `make app` (local dev) does NOT regenerate this; the checked-in "0" stays.
public enum VigilVersion {
    public static let value = "0"
}
